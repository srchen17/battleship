package cs3500.pa03.controller;

/**
 * Represents a piece of input from a manual player.
 */
public interface ManualUserInput {

  /**
   * Reads the data of this piece of data and returns it as a String.
   *
   * @return  String, representing the data.
   */
  String readData();
}
